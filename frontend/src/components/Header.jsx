const Header = ({ usuario, setUsuario }) => {
  return (
    <header className="flex justify-between p-5 w-full bg-red-400">
        <nav className="">
            <ul>

            </ul>
        </nav>
    </header>
  )
}

export default Header